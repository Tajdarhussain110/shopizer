pipeline {
    agent {label 'new-1'} 
    triggers {
        crone('30 17 * * *')
    }
    stages {
        stage('clone') { 
            steps {
                git url:'https://github.com/Tajdarhussain110/shopizer.git' , branch : 'devlop' 
            }
        }
        stage('install') { 
            steps {
                sh 'mvn package'
            }
        }
    }
}