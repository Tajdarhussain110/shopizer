pipeline {
    agent {label 'new-1'} 
    triggers {
        pollSCM('* * * * *')
    }
    stages {
        stage('clone') { 
            steps {
                git url:'https://github.com/Tajdarhussain110/shopizer.git' , branch : 'release' 
            }
        }
        stage('install') { 
            steps {
                sh 'mvn package'
            }
        }
        stage('merge') {
            steps {
                sh 'git merge release devlop' 
            }
        }
    }
}